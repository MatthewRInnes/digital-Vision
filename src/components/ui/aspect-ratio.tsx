/**
 * Aspect Ratio Component
 * 
 * A component for maintaining consistent aspect ratios for content.
 * Built using Radix UI's Aspect Ratio primitive with additional styling.
 * Supports custom ratios and responsive layouts.
 */
import * as React from "react"
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// Aspect ratio component for maintaining content proportions
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
