/**
 * PostCSS Configuration
 * 
 * Configuration file for PostCSS processing pipeline.
 * Includes:
 * - Tailwind CSS for utility-first styling
 * - Autoprefixer for cross-browser compatibility
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
